{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is node server for our node application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kapil",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^2.3.0"
  }
}
